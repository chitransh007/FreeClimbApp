package com.example.freeClimb.Call.moduleApp;

import java.util.Arrays;

import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;
import org.springframework.context.ApplicationContext;

import com.vailsys.freeclimb.api.FreeClimbClient;
import com.vailsys.freeclimb.api.FreeClimbException;

//@SpringBootApplication
public class CallApplication2 {

//	  private final static String fromNumber = System.getenv("FREECLIMB_PHONE_NUMBER");
//	  private final static String toNumber = System.getenv("TO_PHONE_NUMBER");
//	  private final static String accountId = System.getenv("ACCOUNT_ID");
//	  private final static String authToken = System.getenv("AUTH_TOKEN");
//	  private final static String applicationId = System.getenv("APPLICATION_ID");

//    public static void main(String[] args) throws FreeClimbException {
//    	 ApplicationContext ctx = SpringApplication.run(CallApplication2.class, args);
//    
//    System.out.println("Let's inspect the beans provided by Spring Boot:");
//    
//    String[] beanNames = ctx.getBeanDefinitionNames();
//    Arrays.sort(beanNames);
//    for (String beanName : beanNames) {
//      System.out.println(beanName);
//    }
//    FreeClimbClient client2 = new FreeClimbClient(accountId, authToken);
//    client2.calls.create(toNumber, fromNumber, applicationId);
//    Make_outbound_CallController.run();
//  }

}
